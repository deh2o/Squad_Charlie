# Digital Oilfield Monitoring & Predictive Maintenance System
# Install everything with:  pip install -r requirements.txt
#
# NOT listed here because they're part of the Python standard library
# (nothing to install, they just need Python itself):
#   sqlite3, os, random, datetime, smtplib, email.mime.*
#
# tkinter ships with most Python installs, but on some Linux distros
# it needs a system package instead of pip: sudo apt install python3-tk

pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.8
python-dotenv>=1.0
pytest>=7.4
pytest-cov>=4.1